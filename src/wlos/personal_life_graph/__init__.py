"""Personal Life Graph — the system of record.

Entities + relationships + state + temporal context + provenance, modelled
independently of any storage engine so the same domain logic can later run on
PostgreSQL or a graph database without rewriting.
"""
