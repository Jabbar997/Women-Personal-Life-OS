"""Orchestration.

The orchestrator coordinates the six minds. It owns no business domain: it
classifies intent, retrieves context, selects and orders minds, enforces
Guardian, aggregates priorities, routes authorization and emits events.
"""
